module.exports = {
  webpack(config, { dev }) {
    // modify it!
    // config.resolve.alias = {
    //   'react-pdf': 'node_modules/react-pdf/build/entry.webpack',
    // };
    return config;
  },
};
